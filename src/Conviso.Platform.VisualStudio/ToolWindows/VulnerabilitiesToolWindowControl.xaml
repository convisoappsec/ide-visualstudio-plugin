<UserControl
    x:Class="Conviso.Platform.VisualStudio.ToolWindows.VulnerabilitiesToolWindowControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vs="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0"
    Background="{DynamicResource {x:Static vs:VsBrushes.ToolWindowBackgroundKey}}"
    Foreground="{DynamicResource {x:Static vs:VsBrushes.ToolWindowTextKey}}">
    <UserControl.Resources><ResourceDictionary><ResourceDictionary.MergedDictionaries><ResourceDictionary Source="ToolWindowStyles.xaml" /></ResourceDictionary.MergedDictionaries></ResourceDictionary></UserControl.Resources>
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Style="{StaticResource PageTitle}" Text="Vulnerabilities" />
        <WrapPanel Grid.Row="1" Margin="0,0,0,8">
            <ComboBox MinWidth="180" Margin="0,0,8,8" ItemsSource="{Binding Companies}" SelectedItem="{Binding SelectedCompany, Mode=TwoWay}" DisplayMemberPath="DisplayLabel" />
            <ComboBox MinWidth="200" Margin="0,0,8,8" ItemsSource="{Binding Assets}" SelectedItem="{Binding SelectedAsset, Mode=TwoWay}" DisplayMemberPath="DisplayLabel" />
            <Button Content="Refresh" Command="{Binding RefreshCommand}" />
            <TextBlock Margin="4,4,0,8" Style="{StaticResource StatusText}" Text="{Binding Status}" />
        </WrapPanel>
        <Grid Grid.Row="2"><Grid.RowDefinitions><RowDefinition Height="3*"/><RowDefinition Height="8"/><RowDefinition Height="2*"/></Grid.RowDefinitions>
        <DataGrid
            Grid.Row="0"
            AutoGenerateColumns="False"
            IsReadOnly="True"
            ItemsSource="{Binding Items}"
            SelectedItem="{Binding SelectedItem, Mode=TwoWay}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Severity" Binding="{Binding Severity}" Width="110" />
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="130" />
                <DataGridTextColumn Header="Asset" Binding="{Binding AssetName}" Width="180" />
                <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>

        <GridSplitter Grid.Row="1" Height="4" HorizontalAlignment="Stretch" VerticalAlignment="Center" />
        <Border Grid.Row="2" Style="{StaticResource Card}" DataContext="{Binding Details}">
            <StackPanel>
                <TextBlock FontSize="16" FontWeight="SemiBold" Text="{Binding Title}" />
                <TextBlock Margin="0,8,0,0" Text="{Binding Description}" TextWrapping="Wrap" />
                <TextBlock Margin="0,8,0,0" Text="{Binding Severity, StringFormat=Severity: {0}}" />
                <TextBlock Margin="0,4,0,0" Text="{Binding Status, StringFormat=Status: {0}}" />
                <TextBlock Margin="0,4,0,0" Text="{Binding AssetName, StringFormat=Asset: {0}}" />
                <Button Width="120" Margin="0,12,0,0" HorizontalAlignment="Left" Command="{Binding GenerateFixCommand}" Content="Generate Fix" />
                <TextBlock Margin="0,16,0,4" Text="New status" />
                <TextBox Text="{Binding NewStatus, UpdateSourceTrigger=PropertyChanged}" />
                <Button Width="120" Margin="0,12,0,0" HorizontalAlignment="Left" Command="{Binding UpdateStatusCommand}" Content="Update Status" />
                <TextBlock Margin="0,12,0,0" Foreground="Gray" Text="{Binding CommandStatus}" />
            </StackPanel>
        </Border></Grid>
    </Grid>
</UserControl>
