<UserControl
    x:Class="Conviso.Platform.VisualStudio.ToolWindows.PipelineBreaksToolWindowControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vs="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0"
    Background="{DynamicResource {x:Static vs:VsBrushes.ToolWindowBackgroundKey}}"
    Foreground="{DynamicResource {x:Static vs:VsBrushes.ToolWindowTextKey}}">
    <UserControl.Resources><ResourceDictionary><ResourceDictionary.MergedDictionaries><ResourceDictionary Source="ToolWindowStyles.xaml" /></ResourceDictionary.MergedDictionaries></ResourceDictionary></UserControl.Resources>
    <ScrollViewer
        HorizontalScrollBarVisibility="Disabled"
        VerticalScrollBarVisibility="Auto"
        SizeChanged="OnWindowScrollViewerSizeChanged">
    <Grid x:Name="LayoutRoot" Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <WrapPanel Grid.Row="0" Margin="0,0,0,8"><Button Command="{Binding RefreshCommand}"><StackPanel Orientation="Horizontal"><TextBlock Margin="0,0,6,0" FontFamily="Segoe MDL2 Assets" Text="&#xE72C;" /><TextBlock Text="Refresh" /></StackPanel></Button><TextBlock Margin="4,4,0,8" Style="{StaticResource StatusText}" Text="{Binding Status}"/></WrapPanel>
        <Grid Grid.Row="1"><Grid.RowDefinitions><RowDefinition Height="3*"/><RowDefinition Height="8"/><RowDefinition Height="2*"/></Grid.RowDefinitions>
        <DataGrid
            Grid.Row="0"
            AutoGenerateColumns="False"
            IsReadOnly="True"
            ScrollViewer.HorizontalScrollBarVisibility="Auto"
            ItemsSource="{Binding Items}"
            SelectedItem="{Binding SelectedItem, Mode=TwoWay}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="120" />
                <DataGridTextColumn Header="Executed" Binding="{Binding ExecutionDate}" Width="150" />
                <DataGridTextColumn Header="Triggered By" Binding="{Binding TriggeredBy}" Width="160" />
                <DataGridTextColumn Header="Asset" Binding="{Binding AssetName}" Width="240" />
            </DataGrid.Columns>
        </DataGrid>

        <GridSplitter Grid.Row="1" Height="4" HorizontalAlignment="Stretch" VerticalAlignment="Center" />
        <Border Grid.Row="2" Style="{StaticResource Card}">
            <StackPanel>
                <TextBlock FontSize="16" FontWeight="SemiBold" Text="Execution Details" />
                <TextBlock Margin="0,8,0,0" Text="{Binding DetailStatus, StringFormat=Status: {0}}" />
                <TextBlock Margin="0,4,0,0" Text="{Binding DetailExecutionDate, StringFormat=Execution Date: {0}}" TextWrapping="Wrap" />
                <TextBlock Margin="0,4,0,0" Text="{Binding DetailTriggeredBy, StringFormat=Triggered By: {0}}" TextWrapping="Wrap" />
                <TextBlock Margin="0,4,0,0" Text="{Binding DetailSource, StringFormat=Source: {0}}" TextWrapping="Wrap" />
                <TextBlock Margin="0,4,0,0" Text="{Binding DetailAssetName, StringFormat=Asset: {0}}" TextWrapping="Wrap" />
                <TextBlock Margin="0,12,0,4" Text="Reason" FontWeight="SemiBold" />
                <TextBlock Text="{Binding DetailReasonText}" TextWrapping="Wrap" />
                <TextBlock Margin="0,12,0,0" Foreground="Gray" Text="{Binding DetailCommandStatus}" TextWrapping="Wrap" />
            </StackPanel>
        </Border></Grid>
    </Grid>
    </ScrollViewer>
</UserControl>
