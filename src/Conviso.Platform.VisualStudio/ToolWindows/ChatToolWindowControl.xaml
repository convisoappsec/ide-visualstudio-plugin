<UserControl
    x:Class="Conviso.Platform.VisualStudio.ToolWindows.ChatToolWindowControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vs="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0"
    Background="{DynamicResource {x:Static vs:VsBrushes.ToolWindowBackgroundKey}}"
    Foreground="{DynamicResource {x:Static vs:VsBrushes.ToolWindowTextKey}}">
    <UserControl.Resources><ResourceDictionary><ResourceDictionary.MergedDictionaries><ResourceDictionary Source="ToolWindowStyles.xaml" /></ResourceDictionary.MergedDictionaries></ResourceDictionary></UserControl.Resources>
    <ScrollViewer
        x:Name="WindowScrollViewer"
        HorizontalScrollBarVisibility="Disabled"
        VerticalScrollBarVisibility="Auto"
        SizeChanged="OnWindowScrollViewerSizeChanged">
    <Grid x:Name="LayoutRoot" Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Style="{StaticResource StatusText}" Text="{Binding Status}" />
            <WrapPanel Margin="0,8,0,0">
                <Button Command="{Binding ConnectCommand}" Content="Connect Chat" />
                <Button Command="{Binding DisconnectCommand}" Content="Disconnect" />
                <Button Command="{Binding AttachSelectionCommand}" Content="Attach Selection" />
                <Button Command="{Binding AnalyzeSelectionCommand}" Content="Analyze + Suggest Fix" />
                <Button Command="{Binding CheckSimilarIssuesCommand}" Content="Check Similar Issues" />
                <Button Command="{Binding ApplySuggestedFixCommand}" Content="Apply Suggested Fix" />
                <Button Command="{Binding MarkResponseHelpfulCommand}" Content="Mark Helpful" />
                <Button Command="{Binding ClearChatCommand}" Content="Clear Chat" />
            </WrapPanel>
            <Border Margin="0,4,0,0" Style="{StaticResource Card}">
                <DockPanel>
                    <Button DockPanel.Dock="Right" Margin="12,0,0,0" HorizontalAlignment="Right" Command="{Binding ClearAttachedContextCommand}" Content="Clear" />
                    <TextBlock VerticalAlignment="Center" Text="{Binding AttachedContextSummary}" TextWrapping="Wrap" />
                </DockPanel>
            </Border>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" MinHeight="80" />
                <RowDefinition Height="12" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <ListBox
                Grid.Row="0"
                x:Name="TranscriptList"
                MinHeight="100"
                HorizontalContentAlignment="Stretch"
                ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                Background="{DynamicResource {x:Static vs:VsBrushes.ToolWindowBackgroundKey}}"
                Foreground="{DynamicResource {x:Static vs:VsBrushes.ToolWindowTextKey}}"
                ItemsSource="{Binding Transcript}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,0,0,8" Style="{StaticResource Card}">
                            <StackPanel>
                                <TextBlock FontWeight="SemiBold" Text="{Binding Role}" />
                                <TextBlock Margin="0,4,0,0" TextWrapping="Wrap" Text="{Binding Content}" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <TextBox
                Grid.Row="2"
                Margin="0,0,0,12"
                Height="80"
                AcceptsReturn="True"
                TextWrapping="Wrap"
                VerticalScrollBarVisibility="Auto"
                Background="{DynamicResource {x:Static vs:VsBrushes.ToolWindowBackgroundKey}}"
                Foreground="{DynamicResource {x:Static vs:VsBrushes.ToolWindowTextKey}}"
                Text="{Binding Message, UpdateSourceTrigger=PropertyChanged}" />

            <Button
                Grid.Row="3"
                Width="120"
                HorizontalAlignment="Left"
                Command="{Binding SendCommand}"
                Content="Send Message" />
        </Grid>
    </Grid>
    </ScrollViewer>
</UserControl>
