<UserControl
    x:Class="Conviso.Platform.VisualStudio.ToolWindows.SettingsToolWindowControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vs="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0"
    Background="{DynamicResource {x:Static vs:VsBrushes.ToolWindowBackgroundKey}}"
    Foreground="{DynamicResource {x:Static vs:VsBrushes.ToolWindowTextKey}}">
    <UserControl.Resources><ResourceDictionary><ResourceDictionary.MergedDictionaries><ResourceDictionary Source="ToolWindowStyles.xaml" /></ResourceDictionary.MergedDictionaries></ResourceDictionary></UserControl.Resources>
    <Grid Margin="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
        <TextBlock Style="{StaticResource PageTitle}" Text="Conviso Settings" />
        <TabControl Grid.Row="1">
            <TabItem Header="Platform API"><Grid Margin="10"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                <TextBlock Style="{StaticResource FieldLabel}" Text="API Token"/><PasswordBox x:Name="ApiTokenPasswordBox" Grid.Row="1" PasswordChar="*" PasswordChanged="OnApiTokenPasswordChanged"/>
                <TextBlock Grid.Row="2" Style="{StaticResource FieldLabel}" Text="Accessible company"/><ComboBox Grid.Row="3" ItemsSource="{Binding Companies}" SelectedItem="{Binding SelectedCompany, Mode=TwoWay}" DisplayMemberPath="DisplayLabel"/>
                <WrapPanel Grid.Row="4" Margin="0,10,0,0"><Button Command="{Binding LoadCompaniesCommand}" Content="Load Companies"/><Button Command="{Binding ApplySelectedCompanyCommand}" Content="Use Selected Company"/><Button Command="{Binding TestApiCommand}" Content="Test API"/><Button Command="{Binding TestBrokerCommand}" Content="Test Chat"/></WrapPanel>
            </Grid></TabItem>
            <TabItem Header="Scope"><Grid Margin="10"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                <TextBlock Style="{StaticResource FieldLabel}" Text="Company ID"/><TextBox Grid.Row="1" Text="{Binding CompanyId, UpdateSourceTrigger=PropertyChanged}"/>
                <TextBlock Grid.Row="2" Style="{StaticResource FieldLabel}" Text="Requirements Scope ID"/><TextBox Grid.Row="3" Text="{Binding RequirementsScopeId, UpdateSourceTrigger=PropertyChanged}"/>
            </Grid></TabItem>
        </TabControl>
        <DockPanel Grid.Row="2" Margin="0,10,0,0"><WrapPanel DockPanel.Dock="Right"><Button Command="{Binding ResetDefaultsCommand}" Content="Use Defaults"/><Button Command="{Binding SaveCommand}" Content="Save Settings"/></WrapPanel><TextBlock Margin="0,4,8,8" Style="{StaticResource StatusText}" Text="{Binding Status}"/></DockPanel>
    </Grid>
</UserControl>
