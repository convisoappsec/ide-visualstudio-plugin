<UserControl
    x:Class="Conviso.Platform.VisualStudio.ToolWindows.RequirementsToolWindowControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vs="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0"
    Background="{DynamicResource {x:Static vs:VsBrushes.ToolWindowBackgroundKey}}"
    Foreground="{DynamicResource {x:Static vs:VsBrushes.ToolWindowTextKey}}">
    <UserControl.Resources><ResourceDictionary><ResourceDictionary.MergedDictionaries><ResourceDictionary Source="ToolWindowStyles.xaml" /></ResourceDictionary.MergedDictionaries></ResourceDictionary></UserControl.Resources>
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <WrapPanel Grid.Row="0" Margin="0,0,0,8"><Button Command="{Binding RefreshCommand}"><StackPanel Orientation="Horizontal"><TextBlock Margin="0,0,6,0" FontFamily="Segoe MDL2 Assets" Text="&#xE72C;" /><TextBlock Text="Refresh" /></StackPanel></Button><TextBlock Margin="4,4,0,8" Style="{StaticResource StatusText}" Text="{Binding Status}"/></WrapPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.4*" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="1.4*" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="1.8*" />
            </Grid.ColumnDefinitions>

            <GroupBox Grid.Column="0" Header="Projects">
                <DataGrid
                    AutoGenerateColumns="False"
                    IsReadOnly="True"
                    ScrollViewer.HorizontalScrollBarVisibility="Auto"
                    ItemsSource="{Binding Projects}"
                    SelectedItem="{Binding SelectedProject, Mode=TwoWay}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Label" Binding="{Binding Label}" Width="240" />
                        <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="110" />
                        <DataGridTextColumn Header="Type" Binding="{Binding ProjectTypeLabel}" Width="120" />
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="12" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <GroupBox Header="Requirements">
                    <DataGrid
                        AutoGenerateColumns="False"
                        IsReadOnly="True"
                        ScrollViewer.HorizontalScrollBarVisibility="Auto"
                        ItemsSource="{Binding Requirements}"
                        SelectedItem="{Binding SelectedRequirement, Mode=TwoWay}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Label" Binding="{Binding Label}" Width="240" />
                            <DataGridTextColumn Header="Checklist" Binding="{Binding ChecklistTypeLabel}" Width="140" />
                        </DataGrid.Columns>
                    </DataGrid>
                </GroupBox>

                <GroupBox Grid.Row="2" Header="Activities">
                    <DataGrid
                        AutoGenerateColumns="False"
                        IsReadOnly="True"
                        ScrollViewer.HorizontalScrollBarVisibility="Auto"
                        ItemsSource="{Binding Activities}"
                        SelectedItem="{Binding SelectedActivity, Mode=TwoWay}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="240" />
                            <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="110" />
                            <DataGridTextColumn Header="Updated" Binding="{Binding UpdatedAt}" Width="140" />
                        </DataGrid.Columns>
                    </DataGrid>
                </GroupBox>
            </Grid>

            <ScrollViewer Grid.Column="4" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <Border Style="{StaticResource Card}">
                        <StackPanel>
                            <TextBlock FontSize="16" FontWeight="SemiBold" Text="Project Details" />
                            <TextBlock Margin="0,8,0,0" Text="{Binding ProjectLabel}" FontWeight="SemiBold" />
                            <TextBlock Margin="0,4,0,0" Text="{Binding ProjectStatus, StringFormat=Status: {0}}" />
                            <TextBlock Margin="0,4,0,0" Text="{Binding ProjectType, StringFormat=Type: {0}}" />
                            <TextBlock Margin="0,4,0,0" Text="{Binding ProjectAssets, StringFormat=Assets: {0}}" TextWrapping="Wrap" />
                            <TextBlock Margin="0,8,0,0" Text="Goal" FontWeight="SemiBold" />
                            <TextBlock Text="{Binding ProjectGoal}" TextWrapping="Wrap" />
                            <TextBlock Margin="0,8,0,0" Text="Scope" FontWeight="SemiBold" />
                            <TextBlock Text="{Binding ProjectScope}" TextWrapping="Wrap" />
                            <TextBlock Margin="0,8,0,0" Text="Requirements" FontWeight="SemiBold" />
                            <TextBlock Text="{Binding ProjectRequirements}" TextWrapping="Wrap" />
                            <TextBlock Margin="0,16,0,4" Text="New project status" />
                            <TextBox Text="{Binding NewProjectStatus, UpdateSourceTrigger=PropertyChanged}" />
                            <Button Width="160" Margin="0,12,0,0" HorizontalAlignment="Left" Command="{Binding UpdateProjectStatusCommand}">
                                <StackPanel Orientation="Horizontal"><TextBlock Margin="0,0,6,0" FontFamily="Segoe MDL2 Assets" Text="&#xE74E;" /><TextBlock Text="Update Project" /></StackPanel>
                            </Button>
                            <TextBlock Margin="0,12,0,0" Foreground="Gray" Text="{Binding ProjectCommandStatus}" TextWrapping="Wrap" />
                        </StackPanel>
                    </Border>

                    <Border Margin="0,12,0,0" Style="{StaticResource Card}">
                        <StackPanel>
                            <TextBlock FontSize="16" FontWeight="SemiBold" Text="Requirement Details" />
                            <TextBlock Margin="0,8,0,0" Text="{Binding RequirementLabel}" FontWeight="SemiBold" />
                            <TextBlock Margin="0,4,0,0" Text="{Binding RequirementProject, StringFormat=Project: {0}}" />
                            <TextBlock Margin="0,4,0,0" Text="{Binding RequirementChecklistType, StringFormat=Checklist: {0}}" />
                            <TextBlock Margin="0,8,0,0" Text="Checks" FontWeight="SemiBold" />
                            <TextBlock Text="{Binding RequirementChecks}" TextWrapping="Wrap" />
                        </StackPanel>
                    </Border>

                    <Border Margin="0,12,0,0" Style="{StaticResource Card}">
                        <StackPanel>
                            <TextBlock FontSize="16" FontWeight="SemiBold" Text="Activity Details" />
                            <TextBlock Margin="0,8,0,0" Text="{Binding ActivityTitle}" FontWeight="SemiBold" />
                            <TextBlock Margin="0,4,0,0" Text="{Binding ActivityStatus, StringFormat=Status: {0}}" />
                            <TextBlock Margin="0,4,0,0" Text="{Binding ActivityAssignees, StringFormat=Assignees: {0}}" TextWrapping="Wrap" />
                            <TextBlock Margin="0,4,0,0" Text="{Binding ActivityCheck, StringFormat=Check: {0}}" TextWrapping="Wrap" />
                            <TextBlock Margin="0,8,0,0" Text="{Binding ActivityDescription}" TextWrapping="Wrap" />
                            <TextBlock Margin="0,4,0,0" Text="{Binding ActivityUpdatedAt, StringFormat=Updated: {0}}" />
                            <TextBlock Margin="0,16,0,4" Text="New activity status" />
                            <TextBox Text="{Binding NewActivityStatus, UpdateSourceTrigger=PropertyChanged}" />
                            <Button Width="160" Margin="0,12,0,0" HorizontalAlignment="Left" Command="{Binding UpdateActivityStatusCommand}">
                                <StackPanel Orientation="Horizontal"><TextBlock Margin="0,0,6,0" FontFamily="Segoe MDL2 Assets" Text="&#xE74E;" /><TextBlock Text="Update Activity" /></StackPanel>
                            </Button>
                            <TextBlock Margin="0,12,0,0" Foreground="Gray" Text="{Binding ActivityCommandStatus}" TextWrapping="Wrap" />
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
